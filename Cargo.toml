[workspace]
resolver = "2"
members = ["kernel", "runner", "tools/fontgen"]
default-members = ["runner"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = "thin"
codegen-units = 1
